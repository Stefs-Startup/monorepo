{
  "name": "@cmp/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript and tooling configurations for the CMP monorepo",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "echo 'No dev needed for config'",
    "build": "echo 'No build needed for config'",
    "test": "echo 'No tests yet'",
    "lint": "echo 'No lint needed for config'",
    "typecheck": "echo 'No typecheck needed for config'",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^6.0.0"
  }
}