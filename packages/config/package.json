{
  "name": "@cmp/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared configuration files for the monorepo.",
  "scripts": {
    "dev": "echo 'No dev step for config package'",
    "build": "echo 'No build step for config package'",
    "test": "echo 'No tests for config package'",
    "lint": "echo 'No lint step for config package'",
    "typecheck": "echo 'No typecheck step for config package'",
    "clean": "rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "^6.0.0"
  }
}
