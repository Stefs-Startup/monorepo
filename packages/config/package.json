{
  "name": "@cmp/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared configuration for the CMP monorepo",
  "scripts": {
    "dev": "echo 'No dev step for config'",
    "build": "echo 'No build step for config'",
    "test": "echo 'No test step for config'",
    "lint": "echo 'No lint step for config'",
    "typecheck": "echo 'No typecheck step for config'",
    "clean": "echo 'No clean step for config'"
  }
}
