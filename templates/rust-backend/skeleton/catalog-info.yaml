apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: ${{ values.name }} Rust backend project
  annotations:
    github.com/project-slug: owner/${{ values.name }}
spec:
  type: service
  lifecycle: experimental
  owner: user:guest
