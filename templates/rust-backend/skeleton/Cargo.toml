[package]
name = "${{ values.name }}"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
axum = "0.7"

[dev-dependencies]
tower = { version = "0.4", features = ["util"] }
