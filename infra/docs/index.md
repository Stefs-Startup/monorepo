# Infrastructure

Technical documentation for the monorepo infrastructure.

## PostgreSQL

The primary database is PostgreSQL. It is used for persistent storage across various applications.

## Redis

Redis is used as a caching layer and message broker for real-time features.
