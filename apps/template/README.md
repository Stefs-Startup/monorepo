# @cmp/template

Standard application template for the monorepo.

To create a new application, copy this directory to `apps/<new-app-name>` and update the `package.json` name to `@cmp/<new-app-name>`.
