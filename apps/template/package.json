{
  "name": "@cmp/template",
  "version": "0.0.0",
  "private": true,
  "description": "App template",
  "scripts": {
    "dev": "echo 'dev'",
    "build": "echo 'build'",
    "lint": "echo 'lint'",
    "test": "echo 'test'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cmp/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
