# CMP Web

The initial web application template for the CMP monorepo.

## Architecture

This application is built using modern web standards and is designed to be easily extensible.

## Getting Started

To run the application locally, use the following command from the root of the monorepo:

```bash
pnpm --filter cmp-web dev
```
